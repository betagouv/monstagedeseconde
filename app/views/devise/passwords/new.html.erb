<div class='onboarding-card onboarding-card-sm my-4' data-controller="signup">
  <header class="header-account">
    <h1 class="h2 text-center mb-0"><%= t('.forgot_your_password') %></h1>

  </header>

  <div class="body" data-controller="downcase">
    <%= form_with(model: resource,
                  scope: resource_name,
                  url: password_path(resource_name),
                  data: { turbo: false },
                  html: { method: :post }) do |f| %>
      <%= render "layouts/form_errors", resource: resource, resource_name: :user %>
      <p>
        Pour changer votre mot de passe, nous devons d’abord vérifier votre identité.
        Pour cela rappelez-nous l’adresse électronique associé à votre compte.
        </p>
      <%= f.label :email%>  
      <%= f.rg2a_explain_required_asterisk %>
      <%= f.text_field :email,
                       class: 'fr-input',
                       required: true,
                       :'aria-label' => "Adresse électronique",
                       type: :email,
                       maxlength: "100",
                       name: "user[email]",
                       pattern: js_email_pattern,
                       autofocus: true,
                       placeholder:"ex: nom@domaine.fr",
                       id: 'user_email',
                       data: { downcase_target: 'emailField', action: 'change->downcase#change' } %>
      <small class="small form-text text-muted">Recevoir une réponse à une candidature, réinitialiser votre mot de passe, être contacté par un employeur…</small>


      <div class="actions d-flex d-sm-block text-center">
        <%= f.submit "Envoyer", class: "fr-btn" %>
      </div>
    <% end %>
    <hr class="fr-my-2w">
    <%= render "devise/shared/links" %>
  </div>
</div>