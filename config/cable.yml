development:
  # adapter: redis
  # url: redis://localhost:6379/1
  adapter: solid_cable
  connects_to:
    database:
      writing: cable
  polling_interval: 0.1.seconds
  message_retention: 1.day

test:
  adapter: test

staging:
  adapter: postgresql

production:
  adapter: postgresql
