---
# Template configuration
_answers_file: .config/copier/.copier-answers.yml
_skip_if_exists:
  - .config/copier/.copier-answers.yml
_min_copier_version: "8.0.0"

_exclude:
    - .git
    - CHANGELOG.md
    - LICENSE
    - README.md
    - VERSION

_message_after_copy: |
  Your DevSecOps project has been created successfully!

  Next steps:
  1. Review the generated configuration files
  2. Install the required dependencies:
    $ task dev:setup-environment
  3. Start using the DevSecOps pipeline with:
    $ task
